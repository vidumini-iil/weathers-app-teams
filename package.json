{
    "name": "root",
    "private": true,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "clean": "lerna clean",
        "start": "lerna run start --parallel --stream",
        "build": "lerna run --stream  build"
    },
    "workspaces": [
        "packages/*"
    ]
}
